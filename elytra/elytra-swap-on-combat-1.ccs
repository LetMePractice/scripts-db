// author: Deleted User (@Deleted User)
def module elytra-swap-on-combat
def desc "Swap elytra -> chestplate when near players"

on pre_tick if playing if entity_in_range :player 2.0 {
    input key v
}