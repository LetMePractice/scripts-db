// author: Jat (@jat9119)
ion if this works but I have this ```def module w-tap
description When hit a player, tap W once
on left_click {
      if holding #sword {
            if attack_progress >=0.9 {
               if target_entity :player {
                input forward {
               }
            }
         }
       }
     }