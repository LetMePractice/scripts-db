// author: Cat (@cat123go)
module trigger-bot
def desc trigger bot simple and easy look at player hold sword or axe and it will automatically attack 

on tick if playing if target_entity :player if holding #sword,#_axe if attack_progress >=0.9 input left