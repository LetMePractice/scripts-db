// author: !                        KeVin.. (@kevinnnnnnnnnnnnnnnnnnnn)
def module auto-pot
def desc "Auto splash at half HP (falling check)"

on tick 3 if playing if health <= 8 if falling if hotbar_has #splash_potion {
    turn_to polar 90 ~ speed 60 then {
        wait 0.06
        switch #splash_potion
        wait 0.06
        input use
        wait 0.1
        switch #sword
        turn_to polar -10 ~ speed 60
    }
}