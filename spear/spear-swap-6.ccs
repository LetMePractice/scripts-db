// author: sn$k II (@snok_fr)
def module spear-swap

on left_click if playing {
    if holding #wind_charge {
        switch #spear
        wait 0.1
        switch back
    }
    if holding #totem {
        switch #spear
        wait 0.1
        switch back
    }
}