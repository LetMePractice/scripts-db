// author: 69zyx (@69zyx)
def module spearswap
def desc "Swaps to spear, lunges, and swaps back in one key"

on key_press h if playing if_not blocking if hotbar_has #spear {
   switch #spear
   input left
   wait 0.07 switch back
}