// author: NotMelonHere (@johan5019._78525)
def module swapidy
def desc "Swaps to spear, lunges, and swaps back in one key"

on key_press R if playing if_not blocking if hotbar_has #spear {
   switch #spear
   input left
   wait 0.07 switch back
}