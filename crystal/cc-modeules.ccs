// author: Cat (@cat123go)
module cc-modeules
def desc hope is best i can do now :)

on left_click if holding #sword if target_block :obsidian,:bedrock wait 0.03 switch #crystal
on left_click if holding #sword if target_block :sand,#stone wait 0.03 switch #obsidian
on left_click if playing if holding #sword if hotbar_has :obsidian switch #obsidian then input use
on tick if target_block :obsidian if holding #sword if input_active attack wait 0.03 then switch #crystal
on place_block if holding #obsidian switch #sword
I m putting this here for now since i will be testing more tmrw