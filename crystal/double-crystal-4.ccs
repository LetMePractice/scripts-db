// author: Practice (@letmepractice)
module double-crystal
def desc "Use crystal twice when looking at obsidian"

on pre_tick if playing {
   if holding :end_crystal {
      if target_block :obsidian {
         if input_active left {
            loop 2 {
               input use
            }
         }
      }
   }
}