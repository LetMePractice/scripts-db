// author: ccsoured_ (@ccsoured_)
def module safe-walk
def desc "Prevents falling off blocks and auto places block under you"

on post_tick if playing {
    if_not on_ground {
        if inventory_has #block {
            input sneak
            place_block
        }
    }
}