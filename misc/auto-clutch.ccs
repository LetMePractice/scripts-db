// author: LoopholeMC (@loopholemc)
def module auto-clutch
//LoopholeMC
def desc "Turn on (with keybind) while falling and it will automatically clutch for you"

on pre_tick {
    if playing {
         if holding #water {
            if block ~ ~-2 ~ :air 
               if targeting_block { 
                 input use 
                 wait 0.1 input right
                 wait 0.15 input mouse_wheel_up
             }
         }
     }
}
or just do this