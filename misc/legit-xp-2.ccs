// author: unknown
a legit version of fast xp since thats somehow very blatant
 module legit-xp
def desc "windoes exp"

on tick if playing if holding #experience {
   if input_active use if chance_of 33 {
      input use
   }
}